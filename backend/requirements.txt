fastapi>=0.141
uvicorn>=0.53
python-multipart>=0.0.32
# The [asyncio] extra pulls in greenlet, which SQLAlchemy's async engine needs at
# import time. It was present locally by accident, so a clean container was the
# first place its absence showed.
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.22
alembic>=1.20
# The [email] extra pulls in email-validator, which EmailStr needs to build its
# schema; without it the auth models fail at import, not at request time.
pydantic[email]>=2.13
pydantic-settings>=2.15
PyJWT>=2.14
bcrypt>=5.0
httpx>=0.28
websockets>=16.0
google-genai>=2.24
pillow>=12.0
numpy>=2.0
scipy>=1.18
python-dotenv>=1.0
asyncpg>=0.30  # Postgres driver for the hosted database
boto3>=1.36  # S3-compatible client, used against Cloudflare R2
